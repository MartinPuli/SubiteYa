{
  "buildCommand": "npm install && npm run build --workspace=@subiteya/web",
  "outputDirectory": "packages/web/dist",
  "installCommand": "npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
